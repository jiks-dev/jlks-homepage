import styled from "styled-components";

const GitInfoPage: React.FC = () => {
  return <GitInfoWrapper>GitInfoPage</GitInfoWrapper>;
};

const GitInfoWrapper = styled.div`
  height: 100%;
  width: 100%;
  position: relative;
  display: flex;
  align-items: center;
  justify-content: center;
`;

export default GitInfoPage;
